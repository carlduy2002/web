{% extends 'base.html.twig' %}
{% block title %}{% endblock %}
{% block body %}

<body>
<section id="hero">
    <div class="container-fluid">
        <div class="container-mb3" style="min-height: 50vh">
             <div class="row heading">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th style="width:185px;" class="action"><a style="color: #272727;" href="{{path ('add_supplier')}}">
                        <i class="glyphicon glyphicon-plus"></i> Add new Supplier</a>
                </th>
            </tr>
        </thead>
        <tbody>
            {% for s in supplier %}
            <tr>
                <td>{{s.id}}</td>
                <td>{{s.Name}}</td>
                <td>{{s.Email}}</td>
                <td><a style="color: #272727; margin-left:30px;" href="{{path ('edit_supplier', {'id' : s.id})}}" class="glyphicon glyphicon-pencil"></a> 
                <a style="color: #272727; margin-left:30px;" href="{{path ('deleteSupplier', {'id' : s.id})}}" class="glyphicon glyphicon-remove"></a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
</div>
</div>
</div>
</section>
</body>
{% endblock %}
